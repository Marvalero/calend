<%= form_with model: @appointment do |f| %>
	<div>
			<%= f.label :type %>
			<%= f.text_field :type %>
	</div>
	<div>
			<%= f.label :comment %>
			<%= f.text_area :comment %>
	</div>
	<%= f.submit 'Create Appointment' %>
<% end %>